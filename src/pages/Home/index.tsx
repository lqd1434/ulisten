import React from 'react'
import { Box, Button } from '@chakra-ui/react'

const Home = () => {
	return (
		<Box color="white" flexDirection={'column'}>
			home
			<Button>加一</Button>
		</Box>
	)
}

export default Home
